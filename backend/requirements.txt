# Python 3.12 以上が必要（pandas-ta 0.4.x の要件）
yfinance==1.4.1
pandas==3.0.3
numpy==2.2.6
pandas-ta==0.4.71b0
# ローソク足パターン（cdl_pattern）に必要。
# pip wheel が C ライブラリを同梱するため通常は追加手順なしで入る。
# 入らない環境では README の TA-Lib 手順を参照。
TA-Lib==0.6.8
# Phase 1 以降（FastAPI / SQLite API）で使用
fastapi==0.137.0
uvicorn==0.49.0
